<?php ob_start(); ?>
<section class="board-section"><div class="section-heading"><div><span class="eyebrow">EDITORIAL QUEUE</span><h1>Review discovery candidates</h1></div><span class="result-count"><?= count($candidates) ?> pending</span></div><p class="section-intro">Social posts are leads, not proof. Confirm the official event page before converting a candidate into a hackathon.</p><?php if (!$candidates): ?><div class="empty-state"><span class="eyebrow">QUEUE CLEAR</span><h3>No unreviewed candidates.</h3><p>Run the daily Sorsa or X discovery command to collect new leads.</p></div><?php else: ?><div class="card-grid"><?php foreach ($candidates as $candidate): ?><article class="hackathon-card"><div class="card-topline"><span class="eyebrow"><?= e($candidate['source_name'] ?: 'Discovery source') ?></span><span class="status-pill status-open">Unreviewed</span></div><h2><?= e(mb_strimwidth($candidate['text'], 0, 140, '…')) ?></h2><p><?= e($candidate['author_handle'] ? '@' . $candidate['author_handle'] : 'Author not reported') ?></p><a class="button button-primary" href="/candidates/<?= (int) $candidate['id'] ?>">Review candidate</a></article><?php endforeach; ?></div><?php endif; ?></section>
<?php $content = ob_get_clean(); require appRoot() . '/app/Views/layout.php'; ?>
